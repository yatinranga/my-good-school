spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# Url for Server connection with db
spring.datasource.url=jdbc:mysql://localhost/mgs3?createDatabaseIfNotExist=true

#spring.jpa.properties.hibernate.default_schema=efkon2

# Username of sql server
spring.datasource.username=root
# Password of sql server
spring.datasource.password=root@123

#spring.jpa.properties.hibernate.default_schema =

#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

## Hibernate Properties
# It will log sql queries in formatted way if set to true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# This property used to update schema whenever we restart the server
spring.jpa.hibernate.ddl-auto=update
# Improve naming strategy
spring.jpa.properties.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# if true then it will show sql queries in log
spring.jpa.show-sql = true


spring.profiles.active = aws

#management.endpoint.health.enabled=true
management.endpoint.health.show-details=always

#management.endpoints.web.exposure.include=*


#spring.datasource.platform=mysql
mgs.file.upload.location = /tmp/mgs
mgs.s3.bucket = my-good-school
mgs.upload.location = /tmp/mgs

file.upload-dir=D:/nxtlife/mgs

file.supported-extension = .pdf,.docx,.doc,.png,.gif,.jpeg,.jpg, .zip,.PNG,.GIF,.JPEG,.JPG, .xml
file.image-supported-extension = .png,.jpeg,.jpg, .PNG,.JPEG,.JPG

server.port=8082
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.fileSizeThreshold=2KB
# Max file size.
spring.servlet.multipart.maxFileSize=15MB
# Max Request Size
spring.servlet.multipart.maxRequestSize=50MB



#spring.mgs.passwordResetRequestUrl = 

#Mail send properties using SMTP
spring.mail.host=smtp.zoho.com
spring.mail.port=587
spring.mail.username=email.testing@nxtlifetechnologies.com
spring.mail.password=emailtester
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
mail.smtp.debug=true

#default-encoding: UTF-8
#    host: smtp.zoho.com
#    username: <<myemail@company.com>>
#    password: <<password>>
#    port: 465
#    protocol: smtp
#    properties.mail.smtp.socketFactory.class: javax.net.ssl.SSLSocketFactory
#    properties.mail.smtp.socketFactory.fallback: false
#    properties.mail.smtp.startssl.enable: true
#    properties.mail.smtp.socketFactory.port: 465
#    properties.mail.smtp.debug: true
